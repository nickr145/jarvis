# Copy this file to .env and fill in real values. .env is gitignored.

ANTHROPIC_API_KEY=

# Gmail (fill in once email_agent.py is being built)
GMAIL_CREDENTIALS_PATH=./credentials.json

# News sources — optional, only if an agent needs an API key
# rather than plain web search
NEWS_API_KEY=